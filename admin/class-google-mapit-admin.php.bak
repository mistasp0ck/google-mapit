<?php

/**
 * The admin-specific functionality of the plugin.
 *
 * @link       tonystaffiero.com
 * @since      1.0.0
 *
 * @package    Google_Mapit
 * @subpackage Google_Mapit/admin
 */

/**
 * The admin-specific functionality of the plugin.
 *
 * Defines the plugin name, version, and two examples hooks for how to
 * enqueue the admin-specific stylesheet and JavaScript.
 *
 * @package    Google_Mapit
 * @subpackage Google_Mapit/admin
 * @author     Tony Staffiero <me@tonystaffiero.com>
 */
class Google_Mapit_Admin {

	/**
	 * The ID of this plugin.
	 *
	 * @since    1.0.0
	 * @access   private
	 * @var      string    $plugin_name    The ID of this plugin.
	 */
	private $plugin_name;

	/**
	 * The version of this plugin.
	 *
	 * @since    1.0.0
	 * @access   private
	 * @var      string    $version    The current version of this plugin.
	 */
	private $version;

	/**
	 * Options for this plugin.
	 *
	 * @since    1.0.0
	 * @access   private
	 * @var      string    $options    Options for this plugin
	 */
	private $options;

	/**
	 * Meta Fields
	 *
	 * @since    1.0.0
	 * @access   private
	 * @var      array    $location_meta_fields
	 */
	private $location_meta_fields;

	/**
	 * Meta Fields
	 *
	 * @since    1.0.0
	 * @access   private
	 * @var      array    $location_category_meta_fields
	 */
	private $location_category_meta_fields;

	/**
	 * Initialize the class and set its properties.
	 *
	 * @since    1.0.0
	 * @param      string    $plugin_name       The name of this plugin.
	 * @param      string    $version    The version of this plugin.
	 */
	public function __construct( $plugin_name, $version ) {

		// @todo: add depreciation conditional here
		$prefix = 'gmi_';  

		$this->plugin_name = $plugin_name;
		$this->version = $version;
		// $this->taxonomy = $taxonomy;
		$this->options = get_option( 'google_mapit_settings' );

		add_image_size($prefix . 'custom_icon_small', 40,40,true);
		add_image_size($prefix . 'custom-icon_large', 80,80,true);

		define( 'GML_PAGE_MAIN_SLUG', 'google-maps-locator' );

		// Field Array  
		$this->location_meta_fields = array(  
			array(  
			    'label'=> 'Enter your Location',  
			    'desc'  => '',  
			    'name'    => $prefix . 'location',  
			    'type'  => 'location' 
			),  
		    array(  
		        'label'=> 'Address',  
		        'desc'  => '',  
		        'name'    => 'address',  
		        'id' => $prefix . 'street_addy', 
		        'type'  => 'text' 
		    ), 
		    array(  
		        'label'=> 'Address 2',  
		        'desc'  => '',  
		        'name'    => $prefix . 'address2',  
		        'type'  => 'text' 
		    ),  
		    array(  
		        'label'=> 'City',  
		        'desc'  => '',  
		        'name'    => $prefix . 'city',  
		        'id' => 'locality', 
		        'type'  => 'text' 
		    ),  
		    array(  
		        'label'=> 'State',  
		        'desc'  => '',  
		        'name'    => $prefix . 'state',  
		        'id' => 'administrative_area_level_1', 
		        'type'  => 'text' 
		    ),          
		    array(  
		        'label'=> 'Zip Code',  
		        'desc'  => '',  
		        'name'    => $prefix . 'postal',  
		        'id' => 'postal_code', 
		        'type'  => 'text' 
		    ),  
		    array(  
		        'label'=> 'Country',  
		        'desc'  => '',  
		        'name'    => $prefix . 'country',  
		        'id' => 'country', 
		        'type'  => 'text' 
		    ),  
		    array(  
		        'label'=> 'Latitude',  
		        'id'    => $prefix . 'lat',  
		        'name'    => 'latitude', 
		        'type'  => 'text' 
		    ),
		    array(  
		        'label'=> 'Longitude',   
		        'id'    => $prefix . 'lng',  
		        'name'    => 'longitude',  		        
		        'type'  => 'text' 
		    ), 
		    array(  
		        'label'=> 'Phone',  
		        'desc'  => '',  
		        'id'    => $prefix . 'phone',  
		        'name'    => 'phone', 
		        'type'  => 'text' 
		    ),
		    array(  
		        'label'=> 'Link',  
		        'desc'  => 'http://www.example.com',  
		        'id'    => $prefix . 'link',  
		        'name'    => 'link', 
		        'type'  => 'text' 
		    )
		 
		); 

		$this->location_category_meta_fields = array (
			array(  
			    'label'=> 'Add custom Icon',  
			    'desc'  => '',  
			    'name'    => 'custom_icon',  
			    'id' => $prefix . 'custom_icon', 
			    'type'  => 'image' 
			) 

		);

	}

	/**
	 * Register the stylesheets for the admin area.
	 *
	 * @since    1.0.0
	 */
	public function enqueue_styles() {

		/**
		 * This function is provided for demonstration purposes only.
		 *
		 * An instance of this class should be passed to the run() function
		 * defined in Google_Mapit_Loader as all of the hooks are defined
		 * in that particular class.
		 *
		 * The Google_Mapit_Loader will then create the relationship
		 * between the defined hooks and the functions defined in this
		 * class.
		 */

		wp_enqueue_style( $this->plugin_name, plugin_dir_url( __FILE__ ) . 'dist/admin-styles.min.css', array(), $this->version, 'all' );

	}

	/**
	 * Register the JavaScript for the admin area.
	 *
	 * @since    1.0.0
	 */
	public function enqueue_scripts($hook) {

		global $post;

		if ( !empty($post->post_type) && 'location' == $post->post_type && ($hook == 'post.php' || $hook == 'post-new.php') ) {
    
			$api_key = $this->options['google_mapit_text_api_key'];

			$google_maps_url = 'https://maps.googleapis.com/maps/api/js';

			$google_api_url = add_query_arg( array(
			    'key' => $api_key,
			    'libraries' => 'places',
			    'region' => 'US',
			    'callback' => 'initAutocomplete'
			), $google_maps_url );

			wp_enqueue_script( 'google-api', $google_api_url, '', $this->version, true );
			wp_enqueue_script( $this->plugin_name, plugin_dir_url( __FILE__ ) . 'js/google-mapit-admin.js', array(), $this->version, true );


			
			wp_localize_script( 'locator-map', 'googleStorePluginUrl', $vars ); 

		} 
		
		if ($hook == 'edit-tags.php' || $hook == 'term.php') {
			wp_enqueue_script('jquery');
			// This will enqueue the Media Uploader script

			wp_enqueue_media();

			wp_enqueue_script( $this->plugin_name, plugin_dir_url( __FILE__ ) . 'js/google-mapit-admin.js', array(), $this->version, true );
		}

		if ($hook == 'toplevel_page_gmi_general') {
			// This will enqueue the Media Uploader script
			wp_enqueue_media();

			wp_enqueue_style( 'wp-color-picker' );

			wp_enqueue_script( $this->plugin_name, plugin_dir_url( __FILE__ ) . 'js/google-mapit-admin.js', array('wp-color-picker','jquery'), $this->version, true );

		}
	}
	function get_array_val($array, $index = '') {
		(!empty($array[$index])) ? $value = $array[$index] : $value = '';
		
		return $value;
	}

	function google_mapit_add_admin_menu(  ) { 

		add_menu_page( 
			'Google Maps Locator Settings', 
			'Location Map', 
			'manage_options', 
			'gmi_general', array(&$this, 'google_mapit_options_page'),
			'dashicons-location-alt'
		);

		add_submenu_page( 
			'gmi_general', 
			'Settings',
			'Settings', 
			'manage_options', 
			'gmi_general', array(&$this, 'google_mapit_options_page')
		);

		add_submenu_page( 
			// $parent_slug
			'gmi_general', 
			// $page_title
			'Design Options',
			// $menu_title
			'Design Options', 
			// $capability
			'manage_options', 
			// $menu_slug
			'gmi_design_options', 
			// $function
			array(&$this, 'google_mapit_options_page' )
		);

	}

	function google_mapit_design_options_init() {

		register_setting( 'designPage', 'gmi_design_settings' );

		add_settings_section(
			'google_mapit_designPage_section', 
			__( '', 'google_mapit' ), 
			array(&$this, 'google_mapit_settings_section_callback'), 
			// page slug
			'gmi_design_options'
		);

		add_settings_field( 
			'button_color', 
			__( 'Button Color', 'google_mapit' ), 
			array(&$this, 'google_mapit_button_color_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'button_color_hover', 
			__( 'Button Hover Color', 'google_mapit' ), 
			array(&$this, 'google_mapit_button_color_hover_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'button_text_color', 
			__( 'Button Text Color', 'google_mapit' ), 
			array(&$this, 'google_mapit_button_text_color_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'link_text_color', 
			__( 'View All Color', 'google_mapit' ), 
			array(&$this, 'google_mapit_link_text_color_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'google_mapit_text_default_icon', 
			__( 'Default Icon', 'google_mapit' ), 
			array(&$this, 'google_mapit_text_default_icon_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'google_mapit_text_icon_size', 
			__( 'Icon Size', 'google_mapit' ), 
			array(&$this, 'google_mapit_text_icon_size_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);

		add_settings_field( 
			'google_mapit_map_style', 
			__( 'Map Styling (import JSON formatted style)', 'google_mapit' ), 
			array(&$this, 'google_mapit_textarea_map_style_render'), 
			// page slug
			'gmi_design_options', 
			'google_mapit_designPage_section'
		);
	}

	function google_mapit_settings_init(  ) { 

		register_setting( 'pluginPage', 'google_mapit_settings' );

		add_settings_section(
			'google_mapit_pluginPage_section', 
			__( '', 'google_mapit' ), 
			array(&$this, 'google_mapit_settings_section_callback'), 
			// page slug
			'gmi_general'
		);

		add_settings_field( 
			'google_mapit_text_api_key', 
			__( 'Google API Key <a href="https://developers.google.com/maps/documentation/javascript/get-api-key" class="small-link">get key</a>', 'google_mapit' ), 
			array(&$this, 'google_mapit_text_api_key_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section' 
		);

		add_settings_field( 
			'google_mapit_map_height', 
			__( 'Default Map Height', 'google_mapit' ), 
			array(&$this, 'google_mapit_map_height_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'google_mapit_map_width', 
			__( 'Default Map Width', 'google_mapit' ), 
			array(&$this, 'google_mapit_map_width_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'zoom', 
			__( 'Initial Zoom', 'google_mapit' ), 
			array(&$this, 'google_mapit_zoom_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'maxZoom', 
			__( 'Maximum Zoom', 'google_mapit' ), 
			array(&$this, 'google_mapit_max_zoom_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'bounds_padding', 
			__( 'Location Bounds Padding', 'google_mapit' ), 
			array(&$this, 'google_mapit_bounds_padding_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'default_location', 
			__( 'Default Address', 'google_mapit' ), 
			array(&$this, 'google_mapit_default_location_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_section(
			'google_mapit_info_section', 
			__( '', 'google_mapit' ), 
			array(&$this, 'google_mapit_info_section_callback'), 
			// page slug
			'gmi_general'
		);

		add_settings_field( 
			'info_show_directions', 
			__( 'Show directions link?', 'google_mapit' ), 
			array(&$this, 'google_mapit_info_show_directions_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_info_section'
		);

		add_settings_field( 
			'info_show_link', 
			__( 'Show external link?', 'google_mapit' ), 
			array(&$this, 'google_mapit_info_show_link_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_info_section'
		);

		add_settings_field( 
			'info_link_text', 
			__( 'Link Text', 'google_mapit' ), 
			array(&$this, 'google_mapit_info_link_text_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_info_section'
		);

		add_settings_section(
			'google_mapit_list_section', 
			__( '', 'google_mapit' ), 
			array(&$this, 'google_mapit_list_section_callback'), 
			// page slug
			'gmi_general'
		);

		add_settings_field( 
			'list_show_directions', 
			__( 'Show directions link?', 'google_mapit' ), 
			array(&$this, 'google_mapit_list_show_directions_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_list_section'
		);

		add_settings_field( 
			'list_show_link', 
			__( 'Show external link?', 'google_mapit' ), 
			array(&$this, 'google_mapit_list_show_link_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_list_section'
		);

		add_settings_field( 
			'list_link_text', 
			__( 'Link Text', 'google_mapit' ), 
			array(&$this, 'google_mapit_list_link_text_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_list_section'
		);

		add_settings_field( 
			'enable_plugin_api', 
			__( 'Enable Locations API?', 'google_mapit' ), 
			array(&$this, 'google_mapit_enable_api_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

		add_settings_field( 
			'google_mapit_api_url', 
			__( 'External Locations Url', 'google_mapit' ), 
			array(&$this, 'google_mapit_api_url_render'), 
			// page slug
			'gmi_general', 
			'google_mapit_pluginPage_section'
		);

	}


	function google_mapit_text_api_key_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[google_mapit_text_api_key]' value='<?php echo $this->get_array_val($options, 'google_mapit_text_api_key') ?>'>
		<?php

	}

	function google_mapit_enable_api_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[enable_api]' <?php echo ( !empty($options['enable_api']) && $options['enable_api'] == 'on') ? 'checked="checked"' : ''?>>
		<?php

	}


	function google_mapit_api_url_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[google_mapit_api_url]' value='<?php echo $this->get_array_val($options, 'google_mapit_api_url') ?>'>
		<p>Enter the url of the external site you want to pull locations from. <br/><em>note: the site must have the "	
Google Maps Locator" plugin installed and "Enable Plugin API?" setting enabled</em></p>
		<?php

	}

	function google_mapit_enable_single(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[enable_single]' <?php echo ( !emtpy($options['enable_single']) && $options['enable_single'] == 'on') ? 'checked="checked"' : ''?>>
		<?php

	}

	function google_mapit_map_height_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[google_mapit_map_height]' value='<?php echo $this->get_array_val($options, 'google_mapit_map_height') ?>'>
		<?php

	}

	function google_mapit_map_width_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[google_mapit_map_width]' value='<?php echo $this->get_array_val($options, 'google_mapit_map_width') ?>'>
		<?php

	}

	function google_mapit_button_color_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		if ($options['button_color'] != '') {
			$value = $options['button_color'];
		} else {
			$value = '#337ab7';
		}
		?>
		<input type='text' data-default-color="#337ab7" class="colorpicker" name='google_mapit_settings[button_color]' value='<?php echo $value; ?>'>
		<?php

	}

	function google_mapit_button_color_hover_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		if ($options['button_color_hover'] != '') {
			$value = $options['button_color_hover'];
		} else {
			$value = '#286090';
		}
		?>
		<input type='text' data-default-color="#286090" class="colorpicker" name='google_mapit_settings[button_color_hover]' value='<?php echo $value; ?>'>
		<?php

	}

	function google_mapit_button_text_color_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		if ($options['button_text_color'] != '') {
			$value = $options['button_text_color'];
		} else {
			$value = '#ffffff';
		}
		?>
		<input type='text' data-default-color="#ffffff" class="colorpicker" name='google_mapit_settings[button_text_color]' value='<?php echo $value; ?>'>
		<?php

	}

	function google_mapit_link_text_color_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		if ($options['link_text_color'] != '') {
			$value = $options['link_text_color'];
		} else {
			$value = '#ffffff';
		}
		?>
		<input type='text' data-default-color="#ffffff" class="colorpicker" name='google_mapit_settings[link_text_color]' value='<?php echo $value; ?>'>
		<?php

	}


	function google_mapit_text_default_icon_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<?php // Get WordPress' media upload URL
		//$upload_link = esc_url( get_upload_iframe_src( 'image', $post->ID ) );

		// See if there's a media id already saved as post meta
		$img_id = $options['google_mapit_text_default_icon'];

		// Get the image src
		$img_src = wp_get_attachment_image_src( $img_id, 'full' );

		// For convenience, see if the array is valid
		$has_img = is_array( $img_src ); ?>
		  <div>
		      <div style="width: 100%;clear:both;" class="preview meta-img-container">    
		      <?php 
		      $btntext = '';

		      if ( $has_img ) : 
		        $btntext = "Change Image"; ?>
		      <img src="<?php echo $img_src[0] ?>" alt="" class="image-preview" style="max-width:100%" />
		      <?php else :
		        $btntext = "Add Image";
		       ?>
		        
		      <?php endif; ?>
		      </div>
		      <input type="button" name="upload-btn" class="upload-meta-img button-secondary upload-media" value="<?php echo $btntext; ?>"> <input type="button" name="reset-btn" class="reset-btn button-secondary" value="Reset">

		      <!-- A hidden input to set and post the chosen image id -->
		      <input class="meta-img-id" name="google_mapit_settings[google_mapit_text_default_icon]" type="hidden" value="<?php echo esc_attr( $img_id ); ?>" />

		  </div>
		<?php

	}

	function google_mapit_default_location_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<div id="locationField">
	        <input id="autocomplete" name='google_mapit_settings[default_location]' value="<?php echo $this->get_array_val($options, 'default_location') ?>" placeholder="Enter your address" onFocus="geolocate()" type="text"></input>
	    </div>
		<?php

	}
	function google_mapit_zoom_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
    <input name='google_mapit_settings[zoom]' value="<?php echo $this->get_array_val($options, 'zoom') ?>" type="text"></input>
    <p>Zoom Range 0 - 21~ <em>0 is zoomed out all the way</em></p>
		<?php

	}
	function google_mapit_max_zoom_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
    <input name='google_mapit_settings[max_zoom]' value="<?php echo $this->get_array_val($options, 'max_zoom') ?>" type="text"></input>
    <p>Zoom Range 0 - 21~ <em>0 is zoomed out all the way</em></p>
		<?php

	}
		function google_mapit_bounds_padding_render(  ) { 

			$options = get_option( 'google_mapit_settings' );
			?>
	    <input name='google_mapit_settings[bounds_padding]' value="<?php echo $this->get_array_val($options, 'bounds_padding') ?>" type="text"></input>
	    <p>Add Padding around locations (only works with multiple locations) e.g. 50,50,50,50</p>
			<?php

		}

	function google_mapit_text_icon_size_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' class= name='google_mapit_settings[google_mapit_text_icon_size]' value='<?php echo $this->get_array_val($options, 'google_mapit_text_icon_size') ?>'>
		<?php

	}

	function google_mapit_textarea_map_style_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<textarea cols='40' rows='5' name='google_mapit_settings[google_mapit_map_style]'><?php echo $this->get_array_val($options, 'google_mapit_map_style') ?></textarea>
		<?php

	}

	function google_mapit_design_section_callback(  ) { 

		echo __( '<h3>Design Settings</h3>', 'google_mapit' );

	}

	function google_mapit_settings_section_callback(  ) { 

		// echo __( '<h3>Global Settings</h3>', 'google_mapit' );

	}


	function google_mapit_info_section_callback(  ) { 

		echo __( '<h3>Info Window Defaults</h3>', 'google_mapit' );

	}

	function google_mapit_info_show_directions_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[info_show_directions]' value='1' 
		<?php echo ( !empty($options['info_show_directions']) && $options['info_show_directions'] == 1) ? 'checked="checked"' : ''?>>
		<?php

	}

	function google_mapit_info_show_link_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[info_show_link]' value='1' 
		<?php echo ( !empty($options['info_show_link']) && $options['info_show_link'] == 1) ? 'checked="checked"' : ''?>>
		<?php

	}

	function google_mapit_info_link_text_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[info_link_text]' value='<?php echo (!empty($options['info_link_text'])) ? $options['info_link_text'] : ''; ?>'>
		<?php

	}

	function google_mapit_list_section_callback(  ) { 

		echo __( '<h3>Location List Defaults</h3>', 'google_mapit' );

	}

	function google_mapit_list_show_directions_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[list_show_directions]' value='1'
		<?php echo ( !empty($options['list_show_directions']) && $options['list_show_directions'] == 1) ? 'checked="checked"' : ''?>>
		<?php
	}

	function google_mapit_list_show_link_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='checkbox' name='google_mapit_settings[list_show_link]' value='1'
		<?php echo ( !empty($options['list_show_link']) && $options['list_show_link'] == 1) ? 'checked="checked"' : ''?>>
		<?php
	}

	function google_mapit_list_link_text_render(  ) { 

		$options = get_option( 'google_mapit_settings' );
		?>
		<input type='text' name='google_mapit_settings[list_link_text]' value='<?php echo (!empty($options['list_link_text'])) ? $options['list_link_text'] : ''; ?>'>
		<?php

	}


	function google_mapit_options_page( $active_page = '' ) { 

		?>
		<div class="wrap">
			<h2><?php _e( 'Google Maps Locator Settings', 'sandbox' ); ?></h2>
			<?php settings_errors(); ?>
			
			<?php if( isset( $_GET[ 'page' ] ) ) {
				$active_page = $_GET[ 'page' ];

			} else if( $active_page == 'gmi_design_options' ) {
				$active_page = 'gmi_design_options';
			} else {
				$active_page = 'gmi_general';
			} // end if/else ?>
			<?php error_log($active_page); ?>
			<div class="nav-tab-wrapper">
				<a href="?page=gmi_general" class="nav-tab <?php echo $active_page == 'gmi_general' ? 'nav-tab-active' : ''; ?>"><?php _e( 'General Options', 'sandbox' ); ?></a>
				<a href="?page=gmi_design_options" class="nav-tab <?php echo $active_page == 'gmi_design_options' ? 'nav-tab-active' : ''; ?>"><?php _e( 'Design Options', 'sandbox' ); ?></a>
			</div>
			<form class="google-maps-settings-form" action='options.php' method='post'>
				<?php
				
					if( $active_page == 'gmi_design_options' ) {
					
						settings_fields( 'designPage' );
						do_settings_sections( 'gmi_design_options' );
						
					} else {
						// general page
						settings_fields( 'pluginPage' );
						do_settings_sections( 'gmi_general' );
						
					} // end if/else
					
					submit_button();
				
				?>

			</form>
		</div>	
		<?php

	}

	// Locations Posttype
	function create_locations_posttype() {

		$options = get_option( 'google_mapit_settings' );

		
		if (isset($options['enable_api']) && $options['enable_api'] == 'on') {
			$json = true;
		} else {
			$json = false;
		}

		$labels = array(
			'name'               => _x( 'Locations', 'post type general name', 'metro_health' ),
			'singular_name'      => _x( 'Location', 'post type singular name', 'metro_health' ),
			'menu_name'          => _x( 'Locations', 'admin menu', 'metro_health' ),
			'name_admin_bar'     => _x( 'Location', 'add new on admin bar', 'metro_health' ),
			'add_new'            => _x( 'Add New', 'Location', 'metro_health' ),
			'add_new_item'       => __( 'Add New Location', 'metro_health' ),
			'new_item'           => __( 'New Location', 'metro_health' ),
			'edit_item'          => __( 'Edit Location', 'metro_health' ),
			'view_item'          => __( 'View Location', 'metro_health' ),
			'all_items'          => __( 'All Locations', 'metro_health' ),
			'search_items'       => __( 'Search Locations', 'metro_health' ),
			'parent_item_colon'  => __( 'Parent Locations:', 'metro_health' ),
			'not_found'          => __( 'No Locations found.', 'metro_health' ),
			'not_found_in_trash' => __( 'No Locations found in Trash.', 'metro_health' )
		);

		$args = array(
			'labels'             => $labels,
			'description'        => __( 'Description.', 'metro_health' ),
			'public'             => false,
			'publicly_queryable' => true,
			'show_ui'            => true,
			'menu_position'       => 5,
			'menu_icon'          => 'dashicons-location', 
			'show_in_menu'       => true,
			'show_in_admin_bar'   => true,
			'show_in_nav_menus'   => true,    
			'query_var'          => true,
			'rewrite'            => array( 'slug' => 'locations' ),
			'show_in_rest' => $json,
			'capability_type'    => 'post',
			'has_archive'        => true,
			'hierarchical'       => false,
			'menu_position'      => null,
			'supports'           => array( 'title', 'page-attributes', 'custom-fields','meta' )
		);

		register_taxonomy(
			'location-category',
			'location',
			array(
			  'label' => __( 'Categories' ),
			  'show_in_rest' => $json,
			  'rewrite' => array( 'slug' => 'location-category' ),
			  'show_admin_column' => true,
			  'hierarchical' => true,
			)
		);

		register_post_type( 'location', $args );
	}

	// Add the Meta Box for our locations
	function google_mapit_add_location_meta_box() {  
	global $post;

	    add_meta_box(  
	        'location_meta_box', // $id  
	        'Additional Information for the Location', // $title  
	        array($this ,'google_mapit_show_location_meta_box'), // $callback  
	        'location', // $page  
	        'normal', // $context  
	        'high'); // $priority  
		}

	// The location Meta Box Callback  
	function google_mapit_show_location_meta_box() {  

	global $post;
	$meta_fields = $this->location_meta_fields; 

	// Use nonce for verification
	wp_nonce_field( basename( __FILE__ ), 'wpbs_nonce' );
	  
	// Begin the field table and loop
	echo '<table class="form-table">';

	foreach ( $meta_fields as $field ) {
	    // get value of this field if it exists for this post  
	    $meta = get_post_meta($post->ID, $field['name'], true);  
	    // begin a table row with  
	    echo '<tr> 
	            <th><label for="'.$field['id'].'">'.$field['label'].'</label></th> 
	            <td>';  
	            switch($field['type']) {  
	                // text  
	                case 'text':  
	                    echo '<input type="text" class="field" name="'.$field['name'].'" id="'.$field['id'].'" value="'.$meta.'" size="60" /> 
	                        <br /><span class="description">'.$field['desc'].'</span>';  
	                break;

	                // checkbox  
	                case 'checkbox':  
	                    echo '<input type="checkbox" class="checkbox" name="'.$field['name'].'" id="'.$field['id'].'" value="'.$meta.'" size="60" /> 
	                        <br /><span class="description">'.$field['desc'].'</span>';  
	                break;
	                
	                // textarea  
	                case 'textarea':  
	                    echo '<textarea name="'.$field['name'].'" id="'.$field['id'].'" cols="80" rows="4">'.$meta.'</textarea> 
	                        <br /><span class="description">'.$field['desc'].'</span>';  
	                break;  

	                case 'image':
	                ?>
	                <?php // Get WordPress' media upload URL
	                //$upload_link = esc_url( get_upload_iframe_src( 'image', $post->ID ) );

	                // See if there's a media id already saved as post meta
	                $img_id = get_post_meta( $post->ID, $field['id'], true );

	                // Get the image src
	                $img_src = wp_get_attachment_image_src( $img_id, 'full' );

	                // For convenience, see if the array is valid
	                $has_img = is_array( $img_src ); ?>
	                  <div>
	                      <div style="width: 100%;clear:both;" class="preview meta-img-container">    
	                      <?php 
	                      $btntext = '';

	                      if ( $has_img ) : 
	                        $btntext = "Change Image"; ?>
	                      <img src="<?php echo $img_src[0] ?>" alt="" style="max-width:100%;" />
	                      <?php else :
	                        $btntext = "Add Image";
	                       ?>
	                        
	                      <?php endif; ?>
	                      </div>
	                      <input type="button" name="upload-btn" class="upload-meta-img button-secondary upload-media" value="<?php echo $btntext; ?>">

	                      <!-- A hidden input to set and post the chosen image id -->
	                      <input class="meta-img-id" name="<?php echo $field['id']; ?>" type="hidden" value="<?php echo esc_attr( $img_id ); ?>" />

	                  </div>
	                <?php  
	                break;
	            
	            	// location w/ google map
	            	case 'location': ?>
	            	  <style>
	            	    html, body {
	            	      height: 100%;
	            	      margin: 0;
	            	      padding: 0;
	            	    }
	            	    #map {
	            	      height: 100%;
	            	    }
	            	  </style>
	            	  <link type="text/css" rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500">
	            	  <style>
	            	    #locationField, #controls {
	            	      position: relative;
	            	      width: 480px;
	            	    }
	            	    #autocomplete {
	            	      position: absolute;
	            	      top: 0px;
	            	      left: 0px;
	            	      width: 99%;
	            	    }
	            	    .label {
	            	      text-align: right;
	            	      font-weight: bold;
	            	      width: 100px;
	            	      color: #303030;
	            	    }
	            	    #address {
	            	      border: 1px solid #000090;
	            	      background-color: #f0f0ff;
	            	      width: 480px;
	            	      padding-right: 2px;
	            	    }
	            	    #address td {
	            	      font-size: 10pt;
	            	    }
	            	    .field {
	            	      width: 99%;
	            	    }
	            	    .slimField {
	            	      width: 80px;
	            	    }
	            	    .wideField {
	            	      width: 200px;
	            	    }
	            	    #locationField {
	            	      height: 20px;
	            	      margin-bottom: 2px;
	            	    }
	            	  </style>
	            	</head>

	            	<body>
	            	  <div id="locationField">
	            	    <input id="autocomplete" name="location" value="<?php echo $meta; ?>" placeholder="Enter your address" onFocus="geolocate()" type="text"></input>
	            	  </div>

	            	  <script>
	            	    // This example displays an address form, using the autocomplete feature
	            	    // of the Google Places API to help users fill in the information.

	            	    // This example requires the Places library. Include the libraries=places
	            	    // parameter when you first load the API. For example:
	            	    // <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places">

	            	    var placeSearch, autocomplete;
	            	    var componentForm = {
	            	      // street_number: 'short_name',
	            	      street_addy: 'long_name',
	            	      locality: 'long_name',
	            	      administrative_area_level_1: 'short_name',
	            	      postal_code: 'short_name',
	            	      country: 'short_name',
	            	      lat: 'short_name',
	            	      lng: 'short_name'
	            	    };

	            	    function initAutocomplete() {
	            	      // Create the autocomplete object, restricting the search to geographical
	            	      // location types.
	            	      autocomplete = new google.maps.places.Autocomplete(
	            	          /** @type {!HTMLInputElement} */(document.getElementById('autocomplete')),
	            	          {types: ['geocode']});

	            	      // When the user selects an address from the dropdown, populate the address
	            	      // fields in the form.
	            	      autocomplete.addListener('place_changed', fillInAddress);
	            	    }

	            	    function fillInAddress() {
	            	      // Get the place details from the autocomplete object.
	            	      var place = autocomplete.getPlace();

	            	      for (var component in componentForm) {
	            	      	// console.log(component);
	            	        document.getElementById(component).value = '';
	            	        document.getElementById(component).disabled = false;
	            	      }

	            	      // Get each component of the address from the place details
	            	      // and fill the corresponding field on the form.
	            	      for (var i = 0; i < place.address_components.length; i++) {
	            	        var addressType = place.address_components[i].types[0];
							if (componentForm[addressType]) {
	            	          var val = place.address_components[i][componentForm[addressType]];
	            	          document.getElementById(addressType).value = val;
	            	        }
	            	      }
	            	      if(componentForm['street_addy']) {
	            	      	var val = place.name;
	            	      	document.getElementById('street_addy').value = val;
	            	      }

	            	      // Add Latitude and Longitude to hidden fields
	            	      if (componentForm['lat']) {
	            	      	var val = place.geometry.location.lat();
	            	      	document.getElementById('lat').value = val;

	            	      }
	            	      if (componentForm['lng']) {
	            	      	var val = place.geometry.location.lng();
	            	      	document.getElementById('lng').value = val;

	            	      }
	            	    }

	            	    // Bias the autocomplete object to the user's geographical location,
	            	    // as supplied by the browser's 'navigator.geolocation' object.
	            	    function geolocate() {
	            	      if (navigator.geolocation) {
	            	        navigator.geolocation.getCurrentPosition(function(position) {
	            	          var geolocation = {
	            	            lat: position.coords.latitude,
	            	            lng: position.coords.longitude
	            	          };
	            	          var circle = new google.maps.Circle({
	            	            center: geolocation,
	            	            radius: position.coords.accuracy
	            	          });
	            	          autocomplete.setBounds(circle.getBounds());
	            	        });
	            	      }
	            	    }
	            	  </script>

<?php
	            	break;
				} //end switch  
	    echo '</td></tr>';  
	} // end foreach  
	echo '</table>'; // end table  
	}  

	// Save the Data  
	function google_mapit_save_location_meta( $post_id ) {  

	  $meta_fields = $this->location_meta_fields;  

	  // verify nonce  
	  if ( !isset( $_POST['wpbs_nonce'] ) || !wp_verify_nonce($_POST['wpbs_nonce'], basename(__FILE__)) )  
	      return $post_id;

	  // check autosave
	  if ( defined('DOING_AUTOSAVE') && DOING_AUTOSAVE )
	      return $post_id;

	  // check permissions
	  if ( 'page' == $_POST['post_type'] ) {
	      if ( !current_user_can( 'edit_page', $post_id ) )
	          return $post_id;
	      } elseif ( !current_user_can( 'edit_post', $post_id ) ) {
	          return $post_id;
	  }

	  // loop through fields and save the data  
	  foreach ( $meta_fields as $field ) {
	      $old = get_post_meta( $post_id, $field['name'], true );
	      $new = $_POST[$field['name']];

	      if ($new && $new != $old) {
	          update_post_meta( $post_id, $field['name'], $new );
	      } elseif ( '' == $new && $old ) {
	          delete_post_meta( $post_id, $field['name'], $old );
	      }
	  } // end foreach
	}

	function location_category_register_meta() {

		$args = array(
		    'sanitize_callback' => '',
		    'auth_callback' => '',
		    'type' => 'string',
		    'description' => 'Custom icon for category',
		    'single' => true,
		    'show_in_rest' => true,
		);

	    register_meta( 'term', 'custom_icon', $args );
	}

	// Add Icon field for the locations category
	function google_mapit_add_location_category_icon( $term_id) {  

	global $wp_query;

    if (is_category()) {
        $term_id = get_query_var('cat');
    } elseif (is_tax()) {
        $current_term = get_term_by('slug', get_query_var('term'), get_query_var('taxonomy'));
        $term_id = $current_term->term_id;
    }
    $term_id = $term_id->term_id;

	$meta_fields = $this->location_category_meta_fields; 

	// Use nonce for verification
	wp_nonce_field( basename( __FILE__ ), 'category_icon_nonce' );

	// Begin the field table and loop
	echo '<table class="form-table">';

	foreach ( $meta_fields as $field ) {
	    // get value of this field if it exists for this post  
	    $meta = get_term_meta($term_id, $field['name'], true);  
	    // begin a table row with  
	    echo '<tr> 
	            <th><label for="'.$field['id'].'">'.$field['label'].'</label></th> 
	            <td>';  
	            switch($field['type']) {  
	                // text  
	                case 'text':  
	                    echo '<input type="text" class="field" name="'.$field['name'].'" id="'.$field['id'].'" value="'.$meta.'" size="60" /> 
	                        <br /><span class="description">'.$field['desc'].'</span>';  
	                break;
	                
	                // textarea  
	                case 'textarea':  
	                    echo '<textarea name="'.$field['name'].'" id="'.$field['id'].'" cols="80" rows="4">'.$meta.'</textarea> 
	                        <br /><span class="description">'.$field['desc'].'</span>';  
	                break;  

	                case 'image':
	                ?>
	                <?php // Get WordPress' media upload URL
	                //$upload_link = esc_url( get_upload_iframe_src( 'image', $post->ID ) );

	                // See if there's a media id already saved as post meta
	                $img_id = get_term_meta( $term_id, $field['id'], true );

	                // Get the image src
	                $img_src = wp_get_attachment_image_src( $img_id, 'full' );

	                // For convenience, see if the array is valid
	                $has_img = is_array( $img_src ); ?>
	                  <div>
	                      <div style="width: 100%;clear:both;" class="preview meta-img-container">    
	                      <?php 
	                      $btntext = '';

	                      if ( $has_img ) : 
	                        $btntext = "Change Image"; ?>
	                      <img style="width:40px;height:40px;" src="<?php echo $img_src[0] ?>" alt="" style="max-width:100%;" />
	                      <?php else :
	                        $btntext = "Add Image";
	                       ?>
	                        
	                      <?php endif; ?>
	                      </div>
	                      <input type="button" name="upload-btn" class="upload-meta-img button-secondary upload-media" value="<?php echo $btntext; ?>">

	                      <!-- A hidden input to set and post the chosen image id -->
	                      <input class="meta-img-id" name="<?php echo $field['id']; ?>" type="hidden" value="<?php echo esc_attr( $img_id ); ?>" />

	                  </div>
	                <?php  
	                break;

				} //end switch  
	    echo '</td></tr>';  
	} // end foreach  
	echo '</table>'; // end table  
	}  

	function google_mapit_save_location_category_icon( $term_id ) {

	    if ( ! isset( $_POST['category_icon_nonce'] ) || ! wp_verify_nonce( $_POST['category_icon_nonce'], basename( __FILE__ ) ) )
	        return;

	    $old_color = get_term_meta($term_id, $prefix . 'custom_icon' );
	    $new_color = isset( $_POST[$prefix . 'custom_icon'] ) ? $_POST[$prefix . 'custom_icon'] : '';

	    if ( $old_color && '' === $new_color )
	        delete_term_meta( $term_id, $prefix . 'custom_icon' );

	    else if ( $old_color !== $new_color )
	        update_term_meta( $term_id, $prefix . 'custom_icon', $new_color );
	}

}
